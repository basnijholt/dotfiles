# One case-insensitive literal per line: word<TAB>reason
# Blank lines and comments are ignored.
# Optional private rules: ~/.config/git/forbidden-words.private
# example-name<TAB>Explain why this name must not enter a public repository.
